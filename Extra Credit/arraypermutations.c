#include <stdio.h>

/*Logic of the algorithm :
Basically, you fix an element and then constantly decrease the array size 
as you permutate the remaining elements.
It uses a while loop to keep track of the count of swaps 
When the number of swaps =  n, a new permutation has been generated*/

